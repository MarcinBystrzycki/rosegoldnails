<div {{css-transition 'fade-page'}} class="index-view">
    {{#if model.page.carousel}}
        <Carousel @carousel={{model.page.carousel}}
                  @carouselBottomLink={{model.page.carouselBottomLink}}
                  @logo={{model.page.logo}}
                  @showScrollButton={{true}}></Carousel>
    {{/if}}
    <div data-scroll-anchor class="info-panel">
        <div>
            <div class="info-text">
                <p>
                    Lorem ipsum dolor sit amet, consectetur adipiscing elit. Nunc vel.
                </p>
                <button class="btn btn-basic">Sprawdź</button>
            </div>
        </div>
    </div>
    <Gallery @gallery={{model.page.gallery}}></Gallery>
    <Maps @map={{model.page.map}} @address={{model.page.address}} @contact={{model.page.contactInfo}}></Maps>
</div>
